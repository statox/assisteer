type Controls = {
  selectedCategory: string;
  selected: string;
  planetsMode: string;
  graphMode: string;
  curvesMode: string;
  mergeUniquePlanets: boolean;
  showTools: boolean;
};

export type { Controls };
